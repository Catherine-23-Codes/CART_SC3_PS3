preprocess.py should:
   - Contain a function preprocess_image(image_path)
   - Resize image to 224x224
   - Normalize pixel values
   - Convert image to numpy array
   - Expand dimensions so it can be fed to the model.
