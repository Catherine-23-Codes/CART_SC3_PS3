Create Express routes for waste prediction.

Requirements:
- Use Express Router
- Import prediction controller
- Import upload middleware
- Define POST /predict route
- Route should accept an uploaded image
- Call the predictWaste controller
- Export the router