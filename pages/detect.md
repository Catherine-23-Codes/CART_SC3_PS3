Create a Waste Detection page using React and Next.js.

Requirements:
- Page title: "Detect Waste Using AI"
- Centered layout with modern card UI
- Import and display the following components:
   UploadBox
   PredictionCard
   EcoScore
- When an image is uploaded, show preview and pass the image to PredictionCard
- Display EcoScore based on prediction result
- Include loading animation while prediction is processing
- Use Tailwind CSS for styling
- Responsive design for mobile and desktop