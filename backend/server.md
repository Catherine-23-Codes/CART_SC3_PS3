Create a Node.js server entry file using Express for a project called "EcoSort AI".

Requirements:
- Import the Express app from app.js
- Load environment variables using dotenv
- Start the server on process.env.PORT or default 5000
- Log a message when the server starts
- Handle basic server errors
- Use modern Node.js syntax